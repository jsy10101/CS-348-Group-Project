import { Divider, Typography } from '@mui/material'
import React from 'react'

export default function MoveMoney() {
    return (
        <div>
            <Typography variant="h3">Move Money Page</Typography> 
            <Divider />
            <Typography variant="subtitle-1">Choose accounts to transfer money from</Typography>
        </div>
    )
}
